{% extends 'base.html.twig' %}

{% block title %}Hello PagoController!{% endblock %}

{% block body %}
    <style>
        #radios {
            display: flex;
            /*margin: 0 10px;*/
        }
        #radios > div{
            margin: 0px 10px;
        }
        #radios label{

            display: block;
            width: 200px;
            border:2px solid red;
            height: 200px;
            padding: 20px;
            text-align: center;
            line-height: 12;

        }
        input[type="radio"]{
            display: none;
        }
        #radios input:checked + label{
            background-color: blue;
        }
    </style>
    {{ form(form_view) }}
    <div id="radios">
        <div>
            <input type="radio" name="bono" id="basico">
            <label for="basico">Basico</label>

        </div>
        <div>
            <input type="radio" name="bono" id="premium">
            <label for="premium">Premium</label>

        </div>
    </div>
    <script>
        const radios = document.getElementById("radios")
        const form = document.forms[0].appendChild(radios)
        document.forms[0].addEventListener("submit", (e) => {
            e.preventDefault();
            console.log("enviado")
            console.log(e)
        })
    </script>
{% endblock %}
